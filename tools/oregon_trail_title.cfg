MEMORY {
    ZP:     start = $0000, size = $0100, type = rw;
    CODE:   start = $0800, size = $0100, file = %O;
    RODATA: start = $4000, size = $2100, file = "oregon_trail_title_data.bin";
}
SEGMENTS {
    CODE:   load = CODE, type = ro;
    RODATA: load = RODATA, type = ro;
}
